#!user/bin/python
# encoding:utf-8

value ='脚本12'

print value.encode('gbk'), value



# length = len(value.encode('gbk'))
# print length
# utf8_length = len(value.encode('utf-8'))
# length = (utf8_length - length) / 2 + length
# print(length)
